# Jetson training environment notes.
# Install Jetson-compatible torch and torchvision first.
# On Jetson, do not blindly install generic desktop CUDA wheels.

numpy==1.26.1
Pillow>=10,<12
PyYAML>=6,<7
tqdm>=4.66,<5
matplotlib>=3.8,<4
